export default class Fish{

    const FISHCLASS = [
        "catfish",
        "dogfish",
        "tarpon",
        "mackerel",
        "SeaLion"
    ]
    constructor(){
        switch(let x = Math.floor(Math.random()*1000)){
            case 
        }
    }
}